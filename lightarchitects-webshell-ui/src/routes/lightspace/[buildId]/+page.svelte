<script lang="ts">
  // /lightspace/[buildId] — production mode with a specific build scoped.
  // page.params.buildId is read reactively inside Lightspace.svelte via $app/state.
  import Lightspace from '../../../screens/Lightspace.svelte';
</script>
<Lightspace />
