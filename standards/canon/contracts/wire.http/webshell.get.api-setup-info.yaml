# AUTO-GENERATED v3 — quality tier: MULTI_SOURCE (prose=True, doc=True, req_struct=-, resp_struct=SetupInfoResponse)
# Source: lightarchitects-webshell/src/server/mod.rs:1335
# Handler: setup::setup_info

schema_version: la-contracts/v1
id: wire.http.api-setup-info.get
kind: wire.http
version: 0.1.0
status: draft
northstar_pillars:
- P1
operator_intent: Backend configuration info.
observable_outputs:
- Response status 200 (or one of the documented errors) within the duration budget
- 'Response body conforms to the SetupInfoResponse schema (key fields: setup_complete, config, auth_status, resume_session,
  cwd)'
- 'Operator-visible effect: Backend configuration info.'
- AYIN platform.http.request span emitted with {method, path, status, duration_ms}
errors:
- http_status: 400
  condition: malformed request payload
  code: E_BAD_REQUEST
  operator_sees: Request payload invalid — see error message for the failing field
  forbidden: silently dropping invalid fields or coercing types
  severity: MEDIUM
observability:
  required_spans:
  - name: webshell.http.request
    required_metadata:
    - method
    - path
    - status
    - duration_ms
    parent_relationship: root
  trace_context_propagation: true
  duration_budget_ms: 30000
hitl_boundaries:
- none
conformance_test:
  given: webshell running; auth=none; provider-independent for this layer
  when: GET /api/setup/info
  then:
  - assertion: response status is 2xx (or one of the documented errors)
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  fixtures: []
  timeout_seconds: 30
status_per_provider:
  anthropic_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  openai_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  ollama_cloud:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  ollama_local:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  mistral_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  groq_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  openrouter_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
alpha_gate:
  verdict: pass
  reason: Provider-agnostic wire endpoint with intent, error specs, and observability fully declared. Conformance harness
    wiring upgrades verdict from declared-pass to runtime-verified at Phase 4.
wire_http:
  path: /api/setup/info
  method: GET
  auth_required: none
  idempotency: idempotent
  cors_class: same_origin
  sse_streaming: false
  response_schemas:
    '200':
      type: object
      properties:
        setup_complete:
          type: boolean
        config:
          type: object
          properties:
            agent:
              type: object
              description: 'Rust type: AgentKind'
            backend:
              type: string
            model:
              type: string
            ollama_base_url:
              type: string
            api_key_stored:
              type: boolean
          required:
          - agent
          - backend
          - api_key_stored
        auth_status:
          type: object
          properties:
            claude:
              type: object
              properties:
                has_keychain_auth:
                  type: boolean
                has_api_key:
                  type: boolean
                login_method:
                  type: string
                login_source:
                  type: string
              required:
              - has_keychain_auth
              - has_api_key
              - login_method
              - login_source
            codex:
              type: object
              properties:
                has_keychain_auth:
                  type: boolean
                has_api_key:
                  type: boolean
                login_method:
                  type: string
                login_source:
                  type: string
              required:
              - has_keychain_auth
              - has_api_key
              - login_method
              - login_source
            ollama:
              type: object
              properties:
                base_url:
                  type: string
                reachable:
                  type: boolean
              required:
              - base_url
              - reachable
            mistral:
              type: object
              properties:
                has_api_key:
                  type: boolean
                login_source:
                  type: string
              required:
              - has_api_key
            openrouter:
              type: object
              properties:
                has_api_key:
                  type: boolean
                login_source:
                  type: string
              required:
              - has_api_key
            ollama_cloud:
              type: object
              properties:
                has_api_key:
                  type: boolean
                login_source:
                  type: string
              required:
              - has_api_key
            deepseek:
              type: object
              properties:
                has_api_key:
                  type: boolean
                login_source:
                  type: string
              required:
              - has_api_key
            google_vertex:
              type: object
              properties:
                has_service_account:
                  type: boolean
                project_id:
                  type: string
              required:
              - has_service_account
          required:
          - claude
          - codex
          - ollama
          - mistral
          - ollama_cloud
          - deepseek
          - google_vertex
        resume_session:
          type: string
        cwd:
          type: string
      required:
      - setup_complete
      - auth_status
      - cwd
canon_refs:
- canon://webshell-api-surface-v1
