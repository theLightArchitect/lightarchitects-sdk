# AUTO-GENERATED v3 — quality tier: SINGLE_SOURCE (prose=False, doc=True, req_struct=ChatRequest, resp_struct=-)
# Source: lightarchitects-webshell/src/server/mod.rs:1447
# Handler: litellm_chat::chat_handler

schema_version: la-contracts/v1
id: wire.http.api-litellm-chat.post
kind: wire.http
version: 0.1.0
status: draft
northstar_pillars:
- P1
operator_intent: SSE chat stream.
observable_outputs:
- Response status 201 (or one of the documented errors) within the duration budget
- 'Content-Type: text/event-stream is set; each event is a single SSE frame delimited by a blank line'
- Subscriber sees events appear within the first_token_budget_ms of the underlying broadcaster's emission
- 'Operator-visible effect: SSE chat stream.'
- AYIN platform.http.request span emitted with {method, path, status, duration_ms}
errors:
- http_status: 401
  condition: missing or invalid auth credential
  code: E_UNAUTHORIZED
  operator_sees: Sign in or refresh credential — webshell session expired
  forbidden: silent retry that hides the 401 from the operator
  severity: HIGH
observability:
  required_spans:
  - name: webshell.http.request
    required_metadata:
    - method
    - path
    - status
    - duration_ms
    parent_relationship: root
  - name: webshell.sse.frame
    required_metadata:
    - event_name
    - seq
    - bytes
    parent_relationship: child_of_named
    parent_span_name: webshell.http.request
  trace_context_propagation: true
  duration_budget_ms: 3600000
hitl_boundaries:
- none
conformance_test:
  given: webshell running; auth=bearer_token; provider-independent for this layer
  when: POST /api/litellm/chat with body conforming to ChatRequest
  then:
  - assertion: response status is 2xx (or one of the documented errors)
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  - assertion: Content-Type is text/event-stream and at least one event arrives within 10s
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  - assertion: request without a valid credential returns 401 with the E_UNAUTHORIZED code
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  fixtures:
  - ChatRequest.example.json
  timeout_seconds: 60
status_per_provider:
  anthropic_api:
    result: UNTESTED
    evidence_tier: INFERRED
  openai_api:
    result: UNTESTED
    evidence_tier: INFERRED
  ollama_cloud:
    result: UNTESTED
    evidence_tier: INFERRED
  ollama_local:
    result: UNTESTED
    evidence_tier: INFERRED
  mistral_api:
    result: UNTESTED
    evidence_tier: INFERRED
  groq_api:
    result: UNTESTED
    evidence_tier: INFERRED
  openrouter_api:
    result: UNTESTED
    evidence_tier: INFERRED
alpha_gate:
  verdict: deferred
  reason: Provider-dependent endpoint; alpha gate awaits conformance harness exercising each of seven providers in scope.
wire_http:
  path: /api/litellm/chat
  method: POST
  auth_required: bearer_token
  idempotency: non_idempotent
  cors_class: same_origin
  sse_streaming: true
  request_schema:
    type: object
    properties:
      message:
        type: string
      history:
        type: array
        items:
          type: object
          properties:
            role:
              type: string
            content:
              type: string
          required:
          - role
          - content
    required:
    - message
    - history
  response_schemas:
    '201':
      type: object
      description: shape not yet inferred — _audit pending_ until handler returns a typed Json<T>
