# AUTO-GENERATED v3 — quality tier: MULTI_SOURCE (prose=True, doc=True, req_struct=-, resp_struct=MemoryListResponse)
# Source: lightarchitects-webshell/src/server/mod.rs:1193
# Handler: events::soul_routes::hot_memory_handler

schema_version: la-contracts/v1
id: wire.http.api-soul-memory-hot.get
kind: wire.http
version: 0.1.0
status: draft
northstar_pillars:
- P1
operator_intent: Hot memory — recent entries in the ring buffer.
observable_outputs:
- Response status 200 (or one of the documented errors) within the duration budget
- 'Response body conforms to the MemoryListResponse schema (key fields: memos)'
- 'Operator-visible effect: Hot memory — recent entries in the ring buffer.'
- AYIN platform.http.request span emitted with {method, path, status, duration_ms}
errors:
- http_status: 401
  condition: missing or invalid auth credential
  code: E_UNAUTHORIZED
  operator_sees: Sign in or refresh credential — webshell session expired
  forbidden: silent retry that hides the 401 from the operator
  severity: HIGH
observability:
  required_spans:
  - name: webshell.http.request
    required_metadata:
    - method
    - path
    - status
    - duration_ms
    parent_relationship: root
  trace_context_propagation: true
  duration_budget_ms: 30000
hitl_boundaries:
- none
conformance_test:
  given: webshell running; auth=bearer_token; provider-independent for this layer
  when: GET /api/soul/memory/hot
  then:
  - assertion: response status is 2xx (or one of the documented errors)
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  - assertion: request without a valid credential returns 401 with the E_UNAUTHORIZED code
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  fixtures: []
  timeout_seconds: 30
status_per_provider:
  anthropic_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  openai_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  ollama_cloud:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  ollama_local:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  mistral_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  groq_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  openrouter_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
alpha_gate:
  verdict: pass
  reason: Provider-agnostic wire endpoint with intent, error specs, and observability fully declared. Conformance harness
    wiring upgrades verdict from declared-pass to runtime-verified at Phase 4.
wire_http:
  path: /api/soul/memory/hot
  method: GET
  auth_required: bearer_token
  idempotency: idempotent
  cors_class: same_origin
  sse_streaming: false
  response_schemas:
    '200':
      type: object
      properties:
        memos:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              tier:
                type: object
                description: 'Rust type: MemoryTier'
              content:
                type: string
              significance:
                type: number
              sibling:
                type: string
              strands:
                type: array
                items:
                  type: string
              created_at:
                type: string
              source_path:
                type: string
              resonance:
                type: array
                items:
                  type: string
              themes:
                type: array
                items:
                  type: string
              self_defining:
                type: boolean
              entry_type:
                type: string
            required:
            - id
            - tier
            - content
            - significance
            - sibling
            - strands
            - created_at
            - resonance
            - themes
            - self_defining
      required:
      - memos
canon_refs:
- canon://webshell-api-surface-v1
