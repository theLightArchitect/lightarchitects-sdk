# AUTO-GENERATED v3 — quality tier: SINGLE_SOURCE (prose=False, doc=True, req_struct=HitlResolveBody, resp_struct=-)
# Source: lightarchitects-webshell/src/server/mod.rs:1311
# Handler: copilot::copilot_hitl_resolve_handler

schema_version: la-contracts/v1
id: wire.http.api-copilot-hitl-resolve.post
kind: wire.http
version: 0.1.0
status: draft
northstar_pillars:
- P1
- P3
operator_intent: Operator resolves a paused strategy.
observable_outputs:
- Response status 201 (or one of the documented errors) within the duration budget
- 'Operator-visible effect: Operator resolves a paused strategy.'
- AYIN platform.http.request span emitted with {method, path, status, duration_ms}
errors:
- http_status: 401
  condition: missing or invalid auth credential
  code: E_UNAUTHORIZED
  operator_sees: Sign in or refresh credential — webshell session expired
  forbidden: silent retry that hides the 401 from the operator
  severity: HIGH
- http_status: 404
  condition: requested resource not found
  code: E_NOT_FOUND
  operator_sees: Requested resource not found
  forbidden: returning 200 with an empty body in place of 404
  severity: MEDIUM
- http_status: 422
  condition: request well-formed but semantically invalid
  code: E_UNPROCESSABLE
  operator_sees: Request semantically invalid — see field-level error
  forbidden: returning 400 for semantic errors
  severity: MEDIUM
- http_status: 500
  condition: internal server error
  code: E_INTERNAL
  operator_sees: Internal error — message references the request id
  forbidden: returning a stack trace or stderr in the response body
  severity: HIGH
observability:
  required_spans:
  - name: webshell.http.request
    required_metadata:
    - method
    - path
    - status
    - duration_ms
    parent_relationship: root
  trace_context_propagation: true
  duration_budget_ms: 30000
hitl_boundaries:
- none
conformance_test:
  given: webshell running; auth=bearer_token; provider-independent for this layer
  when: POST /api/copilot/hitl/resolve with body conforming to HitlResolveBody
  then:
  - assertion: response status is 2xx (or one of the documented errors)
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  - assertion: request without a valid credential returns 401 with the E_UNAUTHORIZED code
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  fixtures:
  - HitlResolveBody.example.json
  timeout_seconds: 30
status_per_provider:
  anthropic_api:
    result: UNTESTED
    evidence_tier: INFERRED
  openai_api:
    result: UNTESTED
    evidence_tier: INFERRED
  ollama_cloud:
    result: UNTESTED
    evidence_tier: INFERRED
  ollama_local:
    result: UNTESTED
    evidence_tier: INFERRED
  mistral_api:
    result: UNTESTED
    evidence_tier: INFERRED
  groq_api:
    result: UNTESTED
    evidence_tier: INFERRED
  openrouter_api:
    result: UNTESTED
    evidence_tier: INFERRED
alpha_gate:
  verdict: deferred
  reason: Provider-dependent endpoint; alpha gate awaits conformance harness exercising each of seven providers in scope.
wire_http:
  path: /api/copilot/hitl/resolve
  method: POST
  auth_required: bearer_token
  idempotency: non_idempotent
  cors_class: same_origin
  sse_streaming: false
  request_schema:
    type: object
    properties:
      request_id:
        type: string
      session_id:
        type: string
      choice:
        type: integer
      dismissed:
        type: boolean
    required:
    - request_id
    - session_id
    - choice
    - dismissed
  response_schemas:
    '200':
      type: object
      description: shape not yet inferred — _audit pending_ until handler returns a typed Json<T>
    '404':
      type: object
      description: 'error envelope: {error: string, code: string, request_id?: string}'
      properties:
        error:
          type: string
        code:
          type: string
        request_id:
          type: string
    '422':
      type: object
      description: 'error envelope: {error: string, code: string, request_id?: string}'
      properties:
        error:
          type: string
        code:
          type: string
        request_id:
          type: string
    '500':
      type: object
      description: 'error envelope: {error: string, code: string, request_id?: string}'
      properties:
        error:
          type: string
        code:
          type: string
        request_id:
          type: string
