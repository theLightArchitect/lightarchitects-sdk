# AUTO-GENERATED v3 — quality tier: MULTI_SOURCE (prose=True, doc=True, req_struct=-, resp_struct=TaskLogsResponse)
# Source: lightarchitects-webshell/src/server/mod.rs:1361
# Handler: coordination::task_logs

schema_version: la-contracts/v1
id: wire.http.api-coordination-tasks-id-logs.get
kind: wire.http
version: 0.1.0
status: draft
northstar_pillars:
- P1
operator_intent: Task execution logs.
observable_outputs:
- Response status 200 (or one of the documented errors) within the duration budget
- 'Response body conforms to the TaskLogsResponse schema (key fields: id, log_path, tail)'
- 'Operator-visible effect: Task execution logs.'
- AYIN platform.http.request span emitted with {method, path, status, duration_ms}
errors:
- http_status: 401
  condition: missing or invalid auth credential
  code: E_UNAUTHORIZED
  operator_sees: Sign in or refresh credential — webshell session expired
  forbidden: silent retry that hides the 401 from the operator
  severity: HIGH
- http_status: 404
  condition: requested resource not found
  code: E_NOT_FOUND
  operator_sees: Requested resource not found
  forbidden: returning 200 with an empty body in place of 404
  severity: MEDIUM
observability:
  required_spans:
  - name: webshell.http.request
    required_metadata:
    - method
    - path
    - status
    - duration_ms
    parent_relationship: root
  trace_context_propagation: true
  duration_budget_ms: 30000
hitl_boundaries:
- none
conformance_test:
  given: webshell running; auth=bearer_token; provider-independent for this layer
  when: GET /api/coordination/tasks/{id}/logs
  then:
  - assertion: response status is 2xx (or one of the documented errors)
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  - assertion: request without a valid credential returns 401 with the E_UNAUTHORIZED code
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  fixtures: []
  timeout_seconds: 30
status_per_provider:
  anthropic_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  openai_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  ollama_cloud:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  ollama_local:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  mistral_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  groq_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  openrouter_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
alpha_gate:
  verdict: pass
  reason: Provider-agnostic wire endpoint with intent, error specs, and observability fully declared. Conformance harness
    wiring upgrades verdict from declared-pass to runtime-verified at Phase 4.
wire_http:
  path: /api/coordination/tasks/{id}/logs
  method: GET
  auth_required: bearer_token
  idempotency: idempotent
  cors_class: same_origin
  sse_streaming: false
  response_schemas:
    '401':
      type: object
      description: 'error envelope: {error: string, code: string, request_id?: string}'
      properties:
        error:
          type: string
        code:
          type: string
        request_id:
          type: string
    '404':
      type: object
      description: 'error envelope: {error: string, code: string, request_id?: string}'
      properties:
        error:
          type: string
        code:
          type: string
        request_id:
          type: string
    '200':
      type: object
      properties:
        id:
          type: string
        log_path:
          type: string
        tail:
          type: array
          items:
            type: string
      required:
      - id
      - tail
canon_refs:
- canon://webshell-api-surface-v1
