# AUTO-GENERATED v3 — quality tier: MULTI_SOURCE (prose=True, doc=True, req_struct=-, resp_struct=ConvergenceListResponse)
# Source: lightarchitects-webshell/src/server/mod.rs:1229
# Handler: events::soul_routes::convergences_handler

schema_version: la-contracts/v1
id: wire.http.api-soul-convergences.get
kind: wire.http
version: 0.1.0
status: draft
northstar_pillars:
- P1
operator_intent: Cross-entry convergence signals.
observable_outputs:
- Response status 200 (or one of the documented errors) within the duration budget
- 'Response body conforms to the ConvergenceListResponse schema (key fields: convergences, total)'
- 'Operator-visible effect: Cross-entry convergence signals.'
- AYIN platform.http.request span emitted with {method, path, status, duration_ms}
errors:
- http_status: 401
  condition: missing or invalid auth credential
  code: E_UNAUTHORIZED
  operator_sees: Sign in or refresh credential — webshell session expired
  forbidden: silent retry that hides the 401 from the operator
  severity: HIGH
observability:
  required_spans:
  - name: webshell.http.request
    required_metadata:
    - method
    - path
    - status
    - duration_ms
    parent_relationship: root
  trace_context_propagation: true
  duration_budget_ms: 30000
hitl_boundaries:
- none
conformance_test:
  given: webshell running; auth=bearer_token; provider-independent for this layer
  when: GET /api/soul/convergences
  then:
  - assertion: response status is 2xx (or one of the documented errors)
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  - assertion: request without a valid credential returns 401 with the E_UNAUTHORIZED code
    span_witness: webshell.http.request
    evidence_tier_required: VERIFIED
  fixtures: []
  timeout_seconds: 30
status_per_provider:
  anthropic_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  openai_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  ollama_cloud:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  ollama_local:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  mistral_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  groq_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
  openrouter_api:
    result: N/A
    evidence_tier: VERIFIED
    na_reason: wire.http endpoint is below the provider abstraction layer; behaviour is identical regardless of LLM provider
alpha_gate:
  verdict: pass
  reason: Provider-agnostic wire endpoint with intent, error specs, and observability fully declared. Conformance harness
    wiring upgrades verdict from declared-pass to runtime-verified at Phase 4.
wire_http:
  path: /api/soul/convergences
  method: GET
  auth_required: bearer_token
  idempotency: idempotent
  cors_class: same_origin
  sse_streaming: false
  response_schemas:
    '200':
      type: object
      properties:
        convergences:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              weight:
                type: number
              participant_count:
                type: integer
              discovered_by:
                type: string
              label:
                type: string
              created_at:
                type: string
              participants:
                type: array
                items:
                  type: object
                  properties:
                    step_id:
                      type: string
                    title:
                      type: string
                    vault_path:
                      type: string
                    sibling:
                      type: string
                  required:
                  - step_id
                  - sibling
              siblings:
                type: array
                items:
                  type: string
            required:
            - id
            - weight
            - participant_count
            - discovered_by
            - created_at
            - participants
            - siblings
        total:
          type: integer
      required:
      - convergences
      - total
canon_refs:
- canon://webshell-api-surface-v1
