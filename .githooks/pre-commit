#!/usr/bin/env bash
set -e
echo "[1/2] cargo fmt --check"
cargo fmt --check || { echo "FAILED — run 'cargo fmt' to fix"; exit 1; }
echo "[2/2] cargo clippy"
cargo clippy --all-targets --all-features -- -D warnings 2>&1 | head -50 || { echo "FAILED"; exit 1; }
echo "Pre-commit: OK"
